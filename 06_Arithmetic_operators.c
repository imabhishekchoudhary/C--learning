#include<stdio.h>

int main(){
    int a=3;
    int b=2;
    int c=a+b;

    printf("the value of a is %d and the value of b is %d\n and sum is %d\n",a,b,c);
    printf("the remender  a and b is:%d",a%b);
    return 0;
}